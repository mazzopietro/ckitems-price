{% extends 'base.jinja2' %}
{% block content %}

  <h1>Edit Store</h1>
  <hr>
  <div class="form-size">
    <form method='post'>
        <div class="form-group">
            <label for="name">Store name</label>
            <input type="text" class="form-control" id="name" name="name" placeholder="Name" value="{{ store.name }}">
        </div>
        <div class="form-group">
            <label for="url_prefix">Store URL Prefix</label>
            <input type="text" class="form-control" id="url_prefix" name="url_prefix" placeholder="http://..." value="{{ store.url_prefix }}" aria-describedby="storeURLPrefixHelp">
            <small id="storeURLPrefixHelp" class="form-text text-muted">
                What characters so URLs of all item in this online store start with
            </small>
        </div>
        <div class="form-group">
            <label for="tag_name">Tag Name</label>
            <input type="text" class="form-control" id="tag_name" name="tag_name" placeholder="span" value="{{ store.tag_name }}" aria-describedby="storeTagNameHelp">
            <small id="storeTagNameHelp" class="form-text text-muted">
                What is the HTML tag of the items that contains the item price (usually <code>span</code>)?
            </small>
        </div>
        <div class="form-group">
            <label for="query">Search Query</label>
            <input type="text" class="form-control" id="query" name="query" placeholder='{"id": "price-now"}' value='{{ store.query | tojson }}' aria-describedby="queryHelpBlock">
            <small id="queryHelpBlock" class="form-text text-muted">
                What JSON query would we have to do to find the specific tag with the price? Remember to use double quotation marks!
            </small>
        </div>
        <button type="sumbit" class='btn btn-warning'>Update</button>
    </form>
  </div>

{% endblock %}
